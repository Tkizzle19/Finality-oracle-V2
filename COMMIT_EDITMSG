fix: Simplify Vercel configuration for React deployment

- Simplified vercel.json to minimal rewrites configuration
- Added .vercelignore to exclude unnecessary files
- Verified local build works correctly (157KB gzipped)
- Should resolve public/build directory detection issues
