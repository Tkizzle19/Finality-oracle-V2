fix: Update Vercel configuration for React build

- Fixed vercel.json to use @vercel/static-build
- Added vercel-build script to package.json
- Configured proper routing for SPA
- Set distDir to 'build' for React output

Should resolve 'no output named public' build error
